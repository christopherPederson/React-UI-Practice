let CHECKBOX = (props) => {
    return (
        <div className="checkWrapper">
            <input type="checkbox" />
            <h3>{props.title}</h3>
        </div>
    );
};

export default CHECKBOX;
